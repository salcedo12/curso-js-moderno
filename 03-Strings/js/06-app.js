const nani = 'pan bimbo ';


//repetir una cadena de texto
const textus = 'lleve dos y pague dos '.repeat(2);
console.log(`${nani} ${textus}`)

//split permite dividir un string
const zanahoria = 'texto de relleno'
console.log(zanahoria.split(" "));

const zombie = 'esqueleto, wither, papa';
console.log(zombie.split(", "));